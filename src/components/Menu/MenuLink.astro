---
type Props = {
  href: string;
};

const { href } = Astro.props;

const newTab = href.includes("://");
---

<a
  href={href}
  draggable={false}
  target={newTab ? "_blank" : ""}
  rel={newTab ? "noopener noreferrer" : ""}
  class="text-white font-bebas-neue select-none text-4xl font-medium"
>
  <slot />
</a>
