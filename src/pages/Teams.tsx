import TeamsWidget from "../components/TeamsWidget";

export default function Teams() {
  return (
    <div class="w-full h-screen flex items-center">
      <TeamsWidget class="flex w-full h-full" />
    </div>
  );
}
