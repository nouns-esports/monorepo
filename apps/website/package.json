{
  "name": "website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun --env-file=../../.env.development next dev --turbo",
    "build": "bun --env-file=../../.env.production next build",
    "start": "bun --env-file=../../.env.production next start"
  }
}
