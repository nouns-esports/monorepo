{
  "name": "website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "frog": "frog dev",
    "build": "next build",
    "start": "next start",
    "studio:production": "cross-env NODE_ENV=production drizzle-kit studio --host 127.0.0.1",
    "studio:development": "cross-env NODE_ENV=development drizzle-kit studio --host 127.0.0.1",
    "migrate:production": "cross-env NODE_ENV=production drizzle-kit generate && cross-env NODE_ENV=production node -r esbuild-register db/migrate.ts",
    "migrate:development": "cross-env NODE_ENV=development drizzle-kit generate && cross-env NODE_ENV=development node -r esbuild-register db/migrate.ts"
  }
}
